import React, { Component} from 'react'
import SearchPage from './SearchPage'

class Main extends Component {
  render() {
    return (
      <SearchPage />
    )
  }
}

export default Main